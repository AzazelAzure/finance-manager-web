# Copy to .env.local and adjust. See README.md.
VITE_API_BASE_URL=http://127.0.0.1:8000
# When testing against https://jsdevtesting... in production-like DNS, the SPA can target the
# inactive-color API (see src/lib/apiBaseUrl.ts and deploy/BLUEGREEN_SWITCHOVER.md).
# VITE_STAGING_API_BASE_URL=https://api-jsdevtesting.thehivemanager.com
